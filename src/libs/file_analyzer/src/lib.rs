/**
 * File Analyzer
 * - File Size
 * - Number of words (separated by space)
 * - Number of letters
 * - Occurrence of same word
 * - Occurrence of same letter
 * - Find a letter / word and how many times that is.
 */
pub fn run() {
    println!("Welcome to File Analyzer. This will analyze a .txt file and give some stats on it.");
    // let path = "test.txt";
    // let file = utils::read_file(path).expect("No TXT");
    // let cont = utils::file_to_string(file);
    // println!("{}", cont);
}
